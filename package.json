{
  "name": "@trader-bot/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Trader Bot console — vanilla JS + Vite + Material Web Components.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@material/web": "latest"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
